[package]
name = "chess-cat"
version = "0.1.0"
edition = "2024"
description = "CLI to visualize chess boards in your terminal."
license = "MIT"
repository = "https://github.com/ptsouchlos/chess-cat"

[dependencies]
clap    = { version = "4", features = ["derive"] }
colored = "2"
